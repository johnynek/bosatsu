x = 1

def y(x):
  x + 1

z = if x == 1:
  y
else:
  \y -> y + x

struct Record(a, b)

enum Either:
  Left(left)
  Right(right)

enum Option:
  None
  Some(a)

enum List:
  Empty
  Cons(a: a, tail: List[a])

enum Bool:
  True
  False

struct Unit

# e = Some(1)
# match e:
#   Some(x):
#     z(x)
#   None:
#     0

#hc = ffi "scala" "org.bykn.edgemar.Foo.times" Int -> Int

main = z(100)
